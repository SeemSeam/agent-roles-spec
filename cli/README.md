# CLI

The CLI is planned but intentionally deferred.

Agent Roles starts with the RolePack specification. Commands such as
`agent-roles validate`, `agent-roles render`, `agent-roles mount`, and
`agent-roles unmount` will come after the spec stabilizes.
