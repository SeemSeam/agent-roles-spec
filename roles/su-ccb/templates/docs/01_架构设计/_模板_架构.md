---
id: <由系统生成>
title: [系统 / 项目名称] 架构
doc_type: architecture
updated: YYYY-MM-DD
# 可选 scope 元数据:su-init 分层生成会写入;人工手写架构可标注以参与补缺判断。
# architecture_scope: 子系统 slug;系统总架构固定为 overview。
# scope_source_roots: 仓库相对路径数组,必须写单行 inline list,如 ["apps/web", "src-tauri"]。
# 不要写多行 YAML 数组,行级 parser 会 partial。
---

# [系统 / 项目名称] 架构

> 一句话定位:这套架构是干嘛的 ｜ 最后更新: YYYY-MM-DD ｜ 常青文档(持续更新,无生命周期 status)

---

> 📌 **怎么用这份模板**
>
> **第一步:先决定写几份架构文档**
> - 如果整个系统由**多个独立项目 / 可独立部署的部分**组成(典型:**前端、后端、后台管理**各是独立工程)→ **每个独立项目各写一份**(如 `前端架构.md` / `后端架构.md` / `后台管理架构.md`),**另写一份《系统总架构》**串起它们:讲清几个项目怎么协作、各自边界、数据怎么跨项目流。
> - 如果是**单体 / 小系统** → 一份就够,本模板直接填。
>
> **第二步:按复杂度填**
> - 简单架构:填【一 概述、二 整体结构图、五 核心模块、十 边界】就够。
> - 复杂架构(多模块交互、并发、状态机、第三方集成):填全,并多上**时序图 / 状态图**,把关键取舍写细。
>
> 把所有 `[方括号占位]` 换成你的内容,用不上的段落删掉。

---

## 一、概述与定位

> 📌 先框定:这是什么、在系统里扮演什么角色、用什么架构模式。

| 项 | 内容 |
|----|------|
| 名称 | [项目 / 模块名] |
| 定位 | [一句话:它负责什么] |
| 架构模式 | [如:单体多模块 / 微服务 / 前后端分离 / 插件化 …] |
| 覆盖范围 | [这份讲哪一块] |
| 不覆盖 | [明确排除,指向其它架构文档] |

> 📌 **多项目系统在这里标明**:本系统拆成 [前端 / 后端 / 后台管理 …] 几份,本篇是其中的 **[X]**;整体协作见《系统总架构》(见末尾相关文档)。

---

## 二、整体结构(一张图)

> 📌 架构文档的灵魂:一张 ASCII 图,先让人看懂"有哪几层 / 几块、谁连谁、数据往哪流"。

```
[整体结构图。示例(替换成你的):

  ┌──────────────┐
  │   前端 / 入口  │
  └──────┬───────┘
         ▼  接口 / 协议
  ┌──────────────┐
  │    核心层     │
  └──────┬───────┘
         ▼
  ┌──────────────┐
  │  存储 / 基础   │
  └──────────────┘ ]
```

---

## 三、技术栈

> 📌 用到的主要技术,按类列(框架 / 存储 / 通信 / 第三方 …),版本写关键的就行。

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| [框架] | [?] | [?] | [?] |
| [存储] | | | |
| [通信] | | | |

---

## 四、项目结构

> 📌 目录树 + 关键目录职责。让人知道"代码大致长怎样、东西放哪"。

```
[项目目录树,标注每个目录干嘛]
```

| 关键目录 / 模块 | 职责 | 状态 |
|------|------|------|
| [目录] | [干嘛] | [✅ 在用 / ⭐ 重点 / 🚧 开发中 / 停用] |

---

## 五、核心模块 / 组件

> 📌 系统的主要组成块,各自职责和依赖。

| 模块 / 组件 | 职责 | 依赖 |
|------|------|------|
| [模块] | [干嘛] | [依赖谁] |

---

## 六、关键流程 / 数据流

> 📌 挑 1-3 条最重要的链路,用图或时序讲清"一次请求 / 一条数据怎么走完一遍"。

```
[关键流程 / 时序图。示例:
  用户操作 → 入口校验 → 核心处理 → 落存储 → 返回 ]
```

---

## 七、关键机制 / 横切关注点

> 📌 贯穿全局的机制,按项目实际挑需要的写:认证授权、并发 / 事务 / 一致性、缓存、消息队列、错误处理、多租户、实时通信、幂等 …

- **[机制一,如 认证授权]**:[怎么做的]
- **[机制二,如 并发 / 一致性]**:[怎么做的]

---

## 八、约定与规范

> 📌 落地时必须遵守的:命名规则、分层依赖、接口约定、目录约定等。

| 类型 | 规则 | 示例 |
|------|------|------|
| [命名] | [规则] | [示例] |

```
[分层依赖图。示例:Controller → Service → DAO]
```

---

## 九、部署 / 运行

> 📌 怎么跑起来:环境要求、关键配置、构建 / 启动命令。

- 环境要求:[?]
- 构建 / 启动:[?]
- 关键配置:[?]

---

## 十、边界 / 不做项

> 📌 这套架构明确**不**管什么,防止误解和蔓延。

- [不做项一]
- [不做项二]

---

## 十一、相关文档

> 📌 系统其它架构文档、模块规格、决策记录等。

| 想深入 | 看 |
|--------|-----|
| [系统总架构 / 其它项目架构] | [路径] |
| [某模块细节] | [模块规格路径] |
| [某决策] | [决策记录路径] |

---

## 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| YYYY-MM-DD | v1.0 | 初版 |
