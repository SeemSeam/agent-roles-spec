# CCB Checker

`ccb-checker` is an experimental Role for node-level worker checks.

Checker reviews one worker result and rejects missing evidence, hidden
fallback, degradation, and scope shrinkage.

The canonical Role id is `agentroles.ccb_checker`. Suggested aliases are
`ccb-checker`, `ccb_checker`, and `node-checker`.
