# Codex Adapter

Suggested Codex display name: `code_reviewer`.

Keep review replies concise and evidence-first. Do not patch code unless the
caller explicitly starts a separate implementation task. Preserve the same
five-score contract used by every other Host Adapter.
