# CCB Worker

`ccb-worker` is an experimental Role for bounded CCB execution work.

Worker handles one work item and returns evidence. It does not decide global
completion or modify workflow authority.

The canonical Role id is `agentroles.ccb_worker`. Suggested aliases are
`ccb-worker`, `ccb_worker`, and `worker`.
