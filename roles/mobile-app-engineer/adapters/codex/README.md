# Codex Adapter Notes

Codex can mount this Role as `mobile` and project the generic skills into the
provider's skill surface when supported.

Keep generated provider files, installed plugins, device logs, screenshots,
build artifacts, simulator state, and app-store credentials outside Role
source.
