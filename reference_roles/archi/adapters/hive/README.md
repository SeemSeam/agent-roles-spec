# Hive Adapter Notes

Hive may consume this RolePack through a Hive adapter.

The adapter should describe Hive's supported role surfaces and isolation model
without changing the core RolePack format.
