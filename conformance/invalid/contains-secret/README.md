# Invalid Fixture: Contains Secret-Like File

This fixture is invalid because Roles must not contain credentials,
secrets, API keys, or auth tokens.

The included file is a placeholder and does not contain a real secret.
