# Hive Adapter Contract Preview

This adapter contract describes how Hive may consume RolePacks.

Hive should publish a capability profile that describes which role surfaces it
supports, such as native agents, skills, plugin content, tools, MCP, memory,
isolation, mount, and unmount.

Hive runtime details should stay in the Hive adapter rather than the core
RolePack specification.
