# Role Memory

Only use tools that are documented under `tools/`.

If a tool is missing or not configured, report that state instead of inventing
commands.

Do not store project-specific task objectives, progress logs, conversation
summaries, provider state, or project-private data in role memory.
