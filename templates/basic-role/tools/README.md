# Tools

Document tool scripts, install steps, update steps, and diagnostic commands
here.

In the preview spec, this directory is source documentation. It is not an
execution manifest and does not grant permission for a host to run commands
automatically.

Do not hide installer behavior in role memory or prompts.
