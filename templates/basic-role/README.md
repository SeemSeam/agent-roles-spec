# Basic Role Template

Use this template for a minimal Role with metadata and role memory.

Replace the placeholder identity, responsibilities, and boundaries before
publishing a role.

Keep this directory as Role source. Do not add project-specific task progress,
conversation history, mounted instance state, or generated adapter output here.
