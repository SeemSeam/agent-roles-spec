# Adapter Notes

Add host-specific notes here.

Adapter notes should describe how a host may consume this role. They should not
redefine the core Role semantics.

Project-specific instance names, scopes, permission grants, and generated
projection files belong to the host adapter or project binding, not to Role
source.
