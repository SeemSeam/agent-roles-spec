# Tests

Add validation notes, fixtures, or conformance examples here.

At minimum, check that the Role has stable identity fields, role memory, clear
responsibilities and non-goals, no secrets, no runtime state, and no
project-specific progress or mounted instance data.
