# Example Plugin Content

This directory represents host-native plugin files bundled with a Role.

The preview spec does not define one universal plugin format. Host adapters
decide how this content maps into their native surfaces.
