# Role Memory

Use role-contained plugin content only when the host adapter supports it.

If the current host cannot consume the plugin content, continue with the role's
other documented capabilities or report the limitation.

Do not store project-specific task objectives, progress logs, conversation
summaries, provider state, or project-private data in role memory.
