# Role With Plugin Content Template

Use this template when a role carries host-native plugin content inside the
Role directory.

Plugin files are Role source until a host adapter projects them into a
host-native location. Generated projection output should not be written back
into this template.
